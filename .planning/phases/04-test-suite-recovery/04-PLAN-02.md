---
wave: 2
depends_on:
  - plan-01
files_modified:
  - tests/Api/ResourceTransformerTest.php
requirements:
  - TEST-03
  - TEST-04
autonomous: true
created: 2026-05-16
---

# Plan 02: Missing Plugin Dependency Handling

Fix 2 test errors caused by missing `api_base` plugin. ResourceTransformerTest references `FSFramework\Plugins\api_base\Api\Resource\ResourceTransformer` which doesn't exist because the `api_base` plugin is not installed in the codebase.

## must_haves

- `tests/Api/ResourceTransformerTest.php` — does not error with "Class not found" in any test run
- TEST-03 and TEST-04 are either skipped (graceful) or fixed to use an available class

## Verification

```bash
ddev exec php vendor/bin/phpunit tests/Api/ResourceTransformerTest.php
```

Must exit with 0 errors (tests may pass or skip, but not error).

Final gate:
```bash
ddev exec php vendor/bin/phpunit
```
Must show 0 errors (down from 2).

## <threat_model>
- **Threats:** None — test-only change. No production code modified.
- **Data flow:** No data exposure risk.
- **ASVS L1:** Non-applicable (test code only).
</threat_model>

## Tasks

<task id="02-01" type="execute">
<objective>Investigate api_base plugin availability and decide fix strategy</objective>
<context>
`tests/Api/ResourceTransformerTest.php` imports `ResourceTransformer` from `FSFramework\Plugins\api_base\Api\Resource\ResourceTransformer`. The `plugins/api_base/` directory doesn't exist in this codebase. The test is for a class that lives in a plugin. Options: (a) install/mock the plugin, (b) skip the test if the class is not available, (c) move the class to core if it belongs there.
</context>
<read_first>
- tests/Api/ResourceTransformerTest.php
- src/Api/ (check if ResourceTransformer or equivalents exist in core)
</read_first>
<action>
1. Check if `src/Api/` contains a `ResourceTransformer` or equivalent class that can be used instead.
2. If not found, check if `vendor/` has the api_base plugin or if it's a git-subtree plugin.
3. Decision matrix:
   - If ResourceTransformer exists in `src/Api/`: update the import to use the core class
   - If api_base is installable as a dev dependency: add to composer.json require-dev and install
   - Otherwise: add a `class_exists()` guard at the top of the test, with `markTestSkipped()` if the class is unavailable
4. Apply the chosen fix.
</action>
<acceptance_criteria>
- `ddev exec php vendor/bin/phpunit tests/Api/ResourceTransformerTest.php` exits 0 (no errors)
- testFilterWritableFieldsAcceptsApiAlias does not produce "Class not found" error
- testValidateWritableFieldsExecutesSymfonyConstraints does not produce "Class not found" error
- If skipped: clear skip message explaining why (api_base plugin not installed)
- If fixed: both tests pass with correct assertions
</acceptance_criteria>
</task>
