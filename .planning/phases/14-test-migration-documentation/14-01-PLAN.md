---
wave: 1
depends_on:
  - 13-core-trim-dependency-removal
files_modified:
  - phpunit.xml
  - tests/Api/
  - AGENTS.md
  - README.md
  - .cursor/rules/fs-framework-testing.mdc
  - .planning/codebase/STACK.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/TESTING.md
  - plugins/api_base/AGENTS.md
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - DOC-01
  - DOC-02
autonomous: true
created: 2026-05-23
---

# Plan 01: Test Migration & Documentation

Close milestone v0.13.0 by removing the stale core `tests/Api/` suite, confirming API tests live only under `plugins/api_base/tests/`, and aligning root/plugin docs and codebase maps with Phases 12–13 (plugin-owned swagger-php, contracts-only core).

**Prerequisite:** Phases 12–13 complete — plugin vendor supplies `OpenApi\Generator`; root `composer.json` no longer lists `zircote/swagger-php` or `firebase/php-jwt`.

## Current state (pre-execution audit)

| Item | Status |
|------|--------|
| `plugins/api_base/tests/` | `ApiAllowedUserSchemaGuardTest.php`, `ResourceTransformerTest.php` ✓ |
| `tests/Api/` | Empty directory (legacy shell after tests moved to plugin) |
| `phpunit.xml` | Still declares stale `Api` testsuite → `tests/Api` |
| `ChainedAuthAdapterTest.php` | Never existed in workspace — doc drift only |
| Root Plugins suite | Already discovers `plugins/*/tests/**/*Test.php` (includes api_base) |
| `AGENTS.md` / `README.md` | Still list jwt + swagger-php in core deps table |
| `plugins/api_base/AGENTS.md` | Stale note that core swagger removal is “fase posterior” |

## must_haves

- No directory `tests/Api/` under core tree (TEST-01)
- Root `phpunit.xml` has no `Api` testsuite block (TEST-02)
- `ddev exec php vendor/bin/phpunit -c plugins/api_base/phpunit.xml` exits 0 (TEST-03)
- `ddev exec php vendor/bin/phpunit` exits 0; api_base tests run via **Plugins** suite only (TEST-04)
- `AGENTS.md` and `README.md` document plugin install + isolated test command; core deps table excludes swagger-php and firebase/php-jwt (DOC-01)
- `plugins/api_base/AGENTS.md` reflects Phase 13 complete (core no longer ships swagger-php) (DOC-01)
- `.planning/codebase/STACK.md` and `INTEGRATIONS.md` attribute swagger-php to `plugins/api_base/composer.json`; jwt noted removed from core / deferred to consumer plugins (DOC-02)
- `.cursor/rules/fs-framework-testing.mdc` synced with AGENTS test-suite table (instruction parity)

## Verification

```bash
# Preconditions
ddev exec composer install --working-dir=plugins/api_base

# TEST-01/02 — no stale core API test tree
test ! -d tests/Api
grep -q '<testsuite name="Api">' phpunit.xml && exit 1 || true

# TEST-03 — isolated plugin suite
ddev exec php vendor/bin/phpunit -c plugins/api_base/phpunit.xml

# TEST-04 — full root suite (api_base via Plugins suite)
ddev exec php vendor/bin/phpunit

# DOC spot-checks
grep -E 'swagger-php|php-jwt' AGENTS.md README.md | grep -v api_base && exit 1 || true
grep -q 'plugins/api_base/composer.json' .planning/codebase/STACK.md
grep -q 'plugins/api_base' .planning/codebase/INTEGRATIONS.md
```

## Out of scope

- Moving `src/Api/Attribute/*` into api_base (REQUIREMENTS Out of Scope)
- Composer merge-plugin (v2 API-04)
- Adding new API tests — only relocate/cleanup verification
- Changing Plugins suite discovery policy (api_base tests intentionally run twice: isolated + root Plugins)

## <threat_model>
- **Threats:** Very low — doc and PHPUnit config only; no runtime or auth changes.
- **Risk:** Accidentally removing non-API tests or breaking Plugins suite glob.
- **Mitigation:** Delete only empty `tests/Api/`; grep for `tests/Api` references before commit; run both PHPUnit invocations.
- **ASVS L1:** N/A — no new attack surface.
</threat_model>

## Tasks

<task id="01" type="execute">
<objective>TEST-01 / TEST-02: Remove stale core API test directory and phpunit suite</objective>
<context>
Historical `tests/Api/ResourceTransformerTest.php` was moved to `plugins/api_base/tests/` during v0.11 test recovery. Directory is empty but still referenced. `ChainedAuthAdapterTest` exists only in outdated docs — no file to migrate.
</context>
<read_first>
- phpunit.xml
- tests/Api/ (confirm empty)
- plugins/api_base/tests/
- .planning/milestones/v0.11.0-phases/04-test-suite-recovery/04-02-SUMMARY.md
</read_first>
<action>
1. Remove empty directory `tests/Api/` (`rmdir` — do not delete parent `tests/`).
2. Edit root `phpunit.xml`: delete the entire `<testsuite name="Api">` block (lines 27–29).
3. Grep repo for `tests/Api` in active config (exclude `.planning/milestones/` archives) — list any remaining hits for task 03 doc cleanup.
</action>
<acceptance_criteria>
- `tests/Api/` does not exist
- `phpunit.xml` contains Base, Core, Security, Traits, Cache, Plugins — no Api suite
- No `*Test.php` files under core `tests/` import `FSFramework\Plugins\api_base\`
</acceptance_criteria>
</task>

<task id="02" type="execute">
<objective>TEST-03 / TEST-04: Verify both PHPUnit entry points</objective>
<context>
TEST-04 accepts API coverage via Plugins suite auto-discovery. Isolated `-c plugins/api_base/phpunit.xml` validates plugin-owned bootstrap and source includes.
</context>
<read_first>
- plugins/api_base/phpunit.xml
- plugins/api_base/tests/
- .planning/phases/13-core-trim-dependency-removal/13-01-SUMMARY.md (baseline counts)
</read_first>
<action>
Run verification commands from plan header. Record counts in execution notes (expect ~407 root tests, 4 api_base isolated — allow ±skipped if env differs).

If root suite fails only because `plugins/api_base/vendor` missing, run `ddev exec composer install --working-dir=plugins/api_base` and retry.

Do not add duplicate test files to core to “fix” failures.
</action>
<acceptance_criteria>
- `ddev exec php vendor/bin/phpunit -c plugins/api_base/phpunit.xml` exit 0
- `ddev exec php vendor/bin/phpunit` exit 0
- Output lists api_base tests under Plugins suite (e.g. ResourceTransformerTest)
</acceptance_criteria>
</task>

<task id="03" type="execute">
<objective>DOC-01: Update root and plugin AGENTS.md + README.md</objective>
<context>
REST API section in AGENTS.md (≈ line 1006+) is already correct post–Phase 13. Stale areas: test tree diagram, Test Suites table, Symfony Components table, directory tree under `src/Api/` (still shows Middleware/Endpoint runtime paths in core).
Phase 12 intentionally deferred root doc updates; Phase 14 completes them.
</context>
<read_first>
- AGENTS.md (Testing section ~100–185, Directory Structure ~330–385, Symfony Components ~920–940, REST API ~1006+)
- README.md (dependencies table ~195–205)
- plugins/api_base/AGENTS.md (Dependencias + Tests sections)
- .planning/phases/12-plugin-composer-setup/12-CONTEXT.md (D-06 root docs deferred)
</read_first>
<action>
**AGENTS.md**
- Remove `tests/Api/` and `ChainedAuthAdapterTest.php` from test structure diagram.
- Update Test Suites table: drop **Api** row; add note that API tests live in `plugins/api_base/tests/` and run via Plugins suite + isolated `-c plugins/api_base/phpunit.xml`.
- Fix `src/Api/` in directory tree to match contracts-only layout (Attribute/, Auth/Contract/, Exception/) — remove stale Middleware/Endpoint/Resource under core.
- Remove `firebase/php-jwt` and `zircote/swagger-php` from Symfony Components table; add short note under REST API Instalación: `ddev exec composer install --working-dir=plugins/api_base`.
- Add test commands to Testing section mirroring README.

**README.md**
- Remove jwt and swagger-php rows from core Symfony/deps table.
- Add bullet: API deps installed in plugin; test with `ddev exec php vendor/bin/phpunit -c plugins/api_base/phpunit.xml`.

**plugins/api_base/AGENTS.md** (commit in api_base repo if separate git)
- Replace “eliminación del core en fase posterior” with: core no longer lists swagger-php (Phase 13 complete).
- Cross-link: root `ddev exec php vendor/bin/phpunit` discovers plugin tests via Plugins suite.

Sync `.cursor/rules/fs-framework-testing.mdc` Test Suites table with AGENTS.md changes (instruction parity rule).
</action>
<acceptance_criteria>
- AGENTS.md Test Suites table has no `tests/Api/` row
- AGENTS.md Symfony table has no swagger-php or firebase/php-jwt
- README.md deps table has no swagger-php or firebase/php-jwt
- plugins/api_base/AGENTS.md states core swagger-php removal is done
- fs-framework-testing.mdc matches AGENTS test suite documentation
</acceptance_criteria>
</task>

<task id="04" type="execute">
<objective>DOC-02: Update codebase map docs</objective>
<context>
`.planning/codebase/STACK.md` and `INTEGRATIONS.md` dated 2026-05-16 still describe root-owned swagger-php and jwt.
</context>
<read_first>
- .planning/codebase/STACK.md (Key Dependencies, API Documentation)
- .planning/codebase/INTEGRATIONS.md (API Documentation, Authentication)
- .planning/codebase/TESTING.md (Test Suites table — same drift as root docs)
- composer.json (root — confirm packages absent)
- plugins/api_base/composer.json
</read_first>
<action>
**STACK.md**
- Move `zircote/swagger-php` from root Key Dependencies to a new subsection “Plugin-owned (api_base)” pointing at `plugins/api_base/composer.json`.
- Remove or annotate `firebase/php-jwt` as removed from core (v0.13.0); v2 API-05 for OidcProvider consumers.

**INTEGRATIONS.md**
- API Documentation: SDK lives in plugin vendor; generation via `plugins/api_base/model/swagger/SwaggerGenerator.php`.
- Authentication: clarify OIDC optional via separate plugins; core `src/Api/Auth/Contract/` only.

**TESTING.md** (same milestone doc pass — prevents map drift)
- Remove Api suite row; document `plugins/api_base/tests/` + isolated phpunit.xml command.
- Remove ChainedAuthAdapterTest from tree diagram if present.

Update analysis date footers to 2026-05-23 where edited.
</action>
<acceptance_criteria>
- STACK.md does not list swagger-php as root critical dependency
- INTEGRATIONS.md references plugin path for swagger-php
- TESTING.md Api suite row removed
- No contradictory “root composer has swagger-php” statements in edited files
</acceptance_criteria>
</task>

<task id="05" type="execute">
<objective>Close plan: SUMMARY, STATE, REQUIREMENTS traceability</objective>
<context>
Standard GSD closeout for final milestone phase.
</context>
<read_first>
- .planning/STATE.md
- .planning/REQUIREMENTS.md
- .planning/ROADMAP.md
</read_first>
<action>
1. Write `.planning/phases/14-test-migration-documentation/14-01-SUMMARY.md` with verification outputs and commits (panel-ab + api_base if applicable).
2. Update `.planning/STATE.md`: Phase 14 complete, milestone ready for `/gsd-complete-milestone`.
3. Mark TEST-01–04 and DOC-01–02 complete in `.planning/REQUIREMENTS.md`.
4. Update `.planning/ROADMAP.md` Phase 14 status with link to SUMMARY.
</action>
<acceptance_criteria>
- 14-01-SUMMARY.md exists with test counts and doc files touched
- STATE.md shows 3/3 phases complete (100%)
- REQUIREMENTS.md all v1 items checked
</acceptance_criteria>
</task>
